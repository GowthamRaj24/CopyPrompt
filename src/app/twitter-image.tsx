/**
 * Twitter / X Large Card for the homepage. Reuses the homepage OG
 * generator with the taller 1200×675 ratio so X doesn't crop the title.
 */
export { default, alt, contentType } from "./opengraph-image";
export { TWITTER_SIZE as size } from "@/lib/og/tokens";
